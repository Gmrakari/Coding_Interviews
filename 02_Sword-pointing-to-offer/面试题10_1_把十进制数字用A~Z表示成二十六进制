

/*
 * Date:2020-07-18 10：00
 * Author:Gmrakari
 * Project:面试题10_1_把十进制数字用A~Z表示成二十六进制
 * 
 */

#在Excel2003中，用A表示第1列，B表示第2列......Z表示第26列，
#AA表示第27列，AB表示第28列......以此类推。
#请写出一个函数，输入用字母表示的列号编码，输出它是第几列。

#include <iostream>
#include <string>
using namespace std;
int StringToInt(const string& str)
{
	int length = str.length();// 或者str.size()

	if(length < 0)
	{
		cout<<"Invalid input"<<endl;
		return -1;
	}

	int sum = 0;

	for(int i = 0;i < length;i++)
	{
		int temp = str[i] - 'A';
		if(temp >= 26 || temp < 0)
		{
			cout<<"Invalid input"<<endl;
			return -1;			
		}
		sum = sum*26 + temp + 1;
	}
	return sum;
}

int main(int argc, char const *argv[])
{
	string str;
	while(getline(cin,str))// 用cin>>str,不能输入空的字符串
		cout<<str<<":"<<StringToInt(str)<<endl;
	return 0;
}


左移运算符m << n 表示把m左移n位。
左移n位的时候，最左边的n位将被丢弃，同时在最右边补上n个0

#比如
00001010 << 2 = 00101000
10001010 << 3 = 01010000

右移运算符m >> n 表示把m右移n位。
右移n位的时候，最右边的n位将被丢弃。

 / 如果数字是一个无符号数值，则用0填补最左边的n位 （如果数字原先是一个正数，则右移之后在最左边补n个0）
-
 \ 如果数字是一个有符号数值，则用数字的符号位填补最左边的n位 （如果数字原先是负数，则右移之后在最左边补n个1）

00001010 >> 2 = 00000010
10001010 >> 3 = 11110001



